<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title><%= oneDesigner.brandName %></title>
</head>
<body>
    <%- include('../navbar') %>

    <header class="brand-profile-header">
        <img src="<%= oneDesigner.logoUrl %>" alt="<%= oneDesigner.brandName %>" class="brand-profile-logo">
        <h1><%= oneDesigner.brandName %></h1>
        <p class="brand-bio"><%= oneDesigner.about %></p>
    </header>

    <hr class="hr">

    <div class="collection-items">
        <h2>Jewelry by this brand</h2>
        <p><%= brandPieces.length %> pieces available</p>
    </div>

    <div class="catalogue">
        <% brandPieces.forEach((piece) => { %>
            <div class="jewelry">
                <a href="/jewelry/<%= piece._id %>">
                    <img src="<%= piece.imageUrl %>" alt="<%= piece.name %>">
                </a>
                
                <h3><%= piece.name %></h3>
                <p class="price">BHD <%= piece.price %></p>
                
                <a href="/jewelry/<%= piece._id %>" class="view-details">View details</a>
            </div>
        <% }) %>
    </div>
</body>
</html>